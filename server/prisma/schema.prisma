// ============================================================
// DDF — Combined Prisma Schema
// Generated by combine-prisma.js
// ============================================================

generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "sqlite"
  url      = env("DATABASE_URL")
}

// Barcode — generated by DDF Engine

model Barcode {
  id          Int      @id @default(autoincrement())
  barcode          String @unique
  material_code    String
  batch_no         String
  quantity         Float
  status           String
  production_date  DateTime
  work_order_no    Int
  container_id     Int
  remark           String
  

  created_by  Int?
  created_at  DateTime @default(now())
  updated_at  DateTime @updatedAt

  @@map("barcode")
}


// BOMItem — generated by DDF Engine

model BOMItem {
  id          Int      @id @default(autoincrement())
  product_bom_id   Int
  material_code    String
  material_name    String
  quantity         Float
  unit             String
  scrap_rate       Float
  parent_item_id   Int
  
  // Relation: product_bom → ProductBOM

  created_by  Int?
  created_at  DateTime @default(now())
  updated_at  DateTime @updatedAt

  @@map("bom_item")
}


// CodingRule — generated by DDF Engine

model CodingRule {
  id          Int      @id @default(autoincrement())
  rule_code        String @unique
  rule_name        String
  entity_type      String
  prefix           String
  date_format      String
  seq_digits       Int
  seq_strategy     String
  seq_scope        String
  sample_output    String
  status           String
  

  created_by  Int?
  created_at  DateTime @default(now())
  updated_at  DateTime @updatedAt

  @@map("coding_rule")
}


// Container — generated by DDF Engine

model Container {
  id          Int      @id @default(autoincrement())
  container_code   String @unique
  container_name   String
  container_type   String
  status           String
  max_weight_kg    Float
  device_id        Int
  workstation_id   Int
  
  // Relation: device → Device

  created_by  Int?
  created_at  DateTime @default(now())
  updated_at  DateTime @updatedAt

  @@map("container")
}


// DefectCategory — generated by DDF Engine

model DefectCategory {
  id          Int      @id @default(autoincrement())
  category_code    String
  category_name    String
  parent_id        Int
  

  created_by  Int?
  created_at  DateTime @default(now())
  updated_at  DateTime @updatedAt

  @@map("defect_category")
}


// Defect — generated by DDF Engine

model Defect {
  id          Int      @id @default(autoincrement())
  defect_code      String
  defect_name      String
  category_id      Int
  severity         String
  status           String
  

  created_by  Int?
  created_at  DateTime @default(now())
  updated_at  DateTime @updatedAt

  @@map("defect")
}


// DeviceCategory — generated by DDF Engine

model DeviceCategory {
  id          Int      @id @default(autoincrement())
  category_code    String
  category_name    String
  parent_id        Int
  

  created_by  Int?
  created_at  DateTime @default(now())
  updated_at  DateTime @updatedAt

  @@map("device_category")
}


// Device — generated by DDF Engine

model Device {
  id          Int      @id @default(autoincrement())
  device_code      String @unique
  device_name      String
  device_type      String
  category_id      Int
  model            String
  production_line_id Int
  workstation_id   Int
  status           String
  purchase_date    DateTime
  last_maintenance_date DateTime
  
  // Relation: production_line → ProductionLine

  created_by  Int?
  created_at  DateTime @default(now())
  updated_at  DateTime @updatedAt

  @@map("device")
}


// Factory — generated by DDF Engine

model Factory {
  id          Int      @id @default(autoincrement())
  factory_code     String @unique
  factory_name     String
  status           String
  

  created_by  Int?
  created_at  DateTime @default(now())
  updated_at  DateTime @updatedAt

  @@map("org_factory")
}


// InspectionItem — generated by DDF Engine

model InspectionItem {
  id          Int      @id @default(autoincrement())
  item_code        String @unique
  item_name        String
  quality_category_id Int
  data_type        String
  unit             String
  spec_min         Float
  spec_max         Float
  options          String
  method           String
  status           String
  

  created_by  Int?
  created_at  DateTime @default(now())
  updated_at  DateTime @updatedAt

  @@map("inspection_item")
}


// InspectionPlan — generated by DDF Engine

model InspectionPlan {
  id          Int      @id @default(autoincrement())
  plan_code        String @unique
  plan_name        String
  check_type       String
  batch_min        Int
  batch_max        Int
  sample_size      Int
  accept_limit     Int
  reject_limit     Int
  severity         String
  status           String
  

  created_by  Int?
  created_at  DateTime @default(now())
  updated_at  DateTime @updatedAt

  @@map("inspection_plan")
}


// MaterialAttrTemplate — generated by DDF Engine

model MaterialAttrTemplate {
  id          Int      @id @default(autoincrement())
  attr_code        String @unique
  attr_name        String
  attr_type        String
  options          String
  unit             String
  required         Boolean
  status           String
  

  created_by  Int?
  created_at  DateTime @default(now())
  updated_at  DateTime @updatedAt

  @@map("material_attr_template")
}


// MaterialAttrValue — generated by DDF Engine

model MaterialAttrValue {
  id          Int      @id @default(autoincrement())
  material_id      Int
  attr_id          Int
  value            String
  

  created_by  Int?
  created_at  DateTime @default(now())
  updated_at  DateTime @updatedAt

  @@map("material_attr_value")
}


// MaterialCategory — generated by DDF Engine

model MaterialCategory {
  id          Int      @id @default(autoincrement())
  category_code    String
  category_name    String
  level            Int
  parent_id        Int
  

  created_by  Int?
  created_at  DateTime @default(now())
  updated_at  DateTime @updatedAt

  @@map("material_category")
}


// Material — generated by DDF Engine

model Material {
  id          Int      @id @default(autoincrement())
  material_code    String @unique
  material_name    String
  specification    String
  category_id      Int
  unit             String
  material_type    String
  status           String
  

  created_by  Int?
  created_at  DateTime @default(now())
  updated_at  DateTime @updatedAt

  @@map("material")
}


// OperatorSkill — generated by DDF Engine

model OperatorSkill {
  id          Int      @id @default(autoincrement())
  operator_id      Int
  skill_id         Int
  skill_level      String
  certified        Boolean
  expire_date      DateTime
  

  created_by  Int?
  created_at  DateTime @default(now())
  updated_at  DateTime @updatedAt

  @@map("operator_skill")
}


// Operator — generated by DDF Engine

model Operator {
  id          Int      @id @default(autoincrement())
  operator_no      String @unique
  operator_name    String
  user_id          Int
  production_line_id Int
  shift_id         Int
  skill_level      String
  status           String
  phone            String
  hire_date        DateTime
  
  // Relation: production_line → ProductionLine
  // Relation: shift → Shift

  created_by  Int?
  created_at  DateTime @default(now())
  updated_at  DateTime @updatedAt

  @@map("operator")
}


// ProcessParam — generated by DDF Engine

model ProcessParam {
  id          Int      @id @default(autoincrement())
  param_code       String
  param_name       String
  process_step_id  Int
  data_type        String
  unit             String
  spec_min         Float
  spec_max         Float
  options          String
  required         Boolean
  status           String
  

  created_by  Int?
  created_at  DateTime @default(now())
  updated_at  DateTime @updatedAt

  @@map("process_param")
}


// ProcessRoute — generated by DDF Engine

model ProcessRoute {
  id          Int      @id @default(autoincrement())
  route_code       String
  product_code     String
  version          String
  status           String
  total_standard_time Int
  
  // Relation: steps → ProcessStep

  created_by  Int?
  created_at  DateTime @default(now())
  updated_at  DateTime @updatedAt

  @@map("process_route")
}


// ProcessStep — generated by DDF Engine

model ProcessStep {
  id          Int      @id @default(autoincrement())
  process_route_id Int
  step_no          Int
  step_code        String
  step_name        String
  standard_time_minutes Int
  device_type_required String
  workstation_type String
  description      String
  
  // Relation: process_route → ProcessRoute

  created_by  Int?
  created_at  DateTime @default(now())
  updated_at  DateTime @updatedAt

  @@map("process_step")
}


// ProductBOM — generated by DDF Engine

model ProductBOM {
  id          Int      @id @default(autoincrement())
  bom_code         String
  product_code     String
  product_name     String
  version          String
  status           String
  
  // Relation: items → BOMItem

  created_by  Int?
  created_at  DateTime @default(now())
  updated_at  DateTime @updatedAt

  @@map("product_bom")
}


// ProductionLine — generated by DDF Engine

model ProductionLine {
  id          Int      @id @default(autoincrement())
  workshop_id      Int
  line_code        String
  line_name        String
  status           String
  

  created_by  Int?
  created_at  DateTime @default(now())
  updated_at  DateTime @updatedAt

  @@map("org_production_line")
}


// ProductionRecord — generated by DDF Engine

model ProductionRecord {
  id          Int      @id @default(autoincrement())
  record_no        String
  work_order_no    Int
  process_step_id  Int
  operator_id      Int
  device_id        Int
  shift_id         Int
  production_line_id Int
  start_time       DateTime
  end_time         DateTime
  output_quantity  Int
  scrap_quantity   Int
  status           String
  remark           String
  
  // Relation: work_order → WorkOrder
  // Relation: process_step → ProcessStep
  // Relation: operator → Operator
  // Relation: device → Device
  // Relation: shift → Shift

  created_by  Int?
  created_at  DateTime @default(now())
  updated_at  DateTime @updatedAt

  @@map("production_record")
}


// QualityCategory — generated by DDF Engine

model QualityCategory {
  id          Int      @id @default(autoincrement())
  category_code    String
  category_name    String
  level            Int
  parent_id        Int
  status           String
  

  created_by  Int?
  created_at  DateTime @default(now())
  updated_at  DateTime @updatedAt

  @@map("quality_category")
}


// QualityCheck — generated by DDF Engine

model QualityCheck {
  id          Int      @id @default(autoincrement())
  check_no         String
  work_order_no    Int
  production_record_id Int
  check_type       String
  check_name       String
  inspection_item_id Int
  sample_size      Int
  measured_value   Float
  tolerance_min    Float
  tolerance_max    Float
  result           String
  defect_id        Int
  inspector_id     Int
  check_time       DateTime
  remark           String
  
  // Relation: work_order → WorkOrder
  // Relation: inspector → User

  created_by  Int?
  created_at  DateTime @default(now())
  updated_at  DateTime @updatedAt

  @@map("quality_check")
}


// ShiftPattern — generated by DDF Engine

model ShiftPattern {
  id          Int      @id @default(autoincrement())
  pattern_name     String @unique
  description      String
  

  created_by  Int?
  created_at  DateTime @default(now())
  updated_at  DateTime @updatedAt

  @@map("shift_pattern")
}


// Shift — generated by DDF Engine

model Shift {
  id          Int      @id @default(autoincrement())
  shift_code       String
  shift_name       String
  start_time       String
  end_time         String
  shift_date       DateTime
  shift_pattern_id Int
  

  created_by  Int?
  created_at  DateTime @default(now())
  updated_at  DateTime @updatedAt

  @@map("shift")
}


// Skill — generated by DDF Engine

model Skill {
  id          Int      @id @default(autoincrement())
  skill_code       String @unique
  skill_name       String
  category         String
  description      String
  status           String
  

  created_by  Int?
  created_at  DateTime @default(now())
  updated_at  DateTime @updatedAt

  @@map("skill")
}


// User — generated by DDF Engine

model User {
  id          Int      @id @default(autoincrement())
  username         String @unique
  real_name        String
  password_hash    String
  role             String
  status           String
  

  created_by  Int?
  created_at  DateTime @default(now())
  updated_at  DateTime @updatedAt

  @@map("sys_user")
}


// WOBOMItem — generated by DDF Engine

model WOBOMItem {
  id          Int      @id @default(autoincrement())
  work_order_no    Int
  material_code    String
  material_name    String
  required_qty     Float
  consumed_qty     Float
  unit             String
  

  created_by  Int?
  created_at  DateTime @default(now())
  updated_at  DateTime @updatedAt

  @@map("wo_bom_item")
}


// WorkOrder — generated by DDF Engine

model WorkOrder {
  id          Int      @id @default(autoincrement())
  order_no         String
  product_code     String
  product_name     String
  quantity         Int
  completed_quantity Int
  scrap_quantity   Int
  status           String
  priority         String
  plan_start_date  DateTime
  plan_end_date    DateTime
  actual_start_time DateTime
  actual_end_time  DateTime
  route_id         Int
  production_line_id Int
  shift_id         Int
  comment          String
  
  // Relation: bom_items → WOBOMItem
  // Relation: production_records → ProductionRecord
  // Relation: quality_checks → QualityCheck
  // Relation: creator → User
  // Relation: production_line → ProductionLine

  created_by  Int?
  created_at  DateTime @default(now())
  updated_at  DateTime @updatedAt

  @@map("work_order")
}


// Workshop — generated by DDF Engine

model Workshop {
  id          Int      @id @default(autoincrement())
  factory_id       Int
  workshop_code    String
  workshop_name    String
  status           String
  

  created_by  Int?
  created_at  DateTime @default(now())
  updated_at  DateTime @updatedAt

  @@map("org_workshop")
}


// Workstation — generated by DDF Engine

model Workstation {
  id          Int      @id @default(autoincrement())
  station_code     String
  station_name     String
  production_line_id Int
  status           String
  
  // Relation: production_line → ProductionLine
  // Relation: devices → Device

  created_by  Int?
  created_at  DateTime @default(now())
  updated_at  DateTime @updatedAt

  @@map("workstation")
}


