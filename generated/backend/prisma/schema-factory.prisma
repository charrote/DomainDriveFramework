// ============================================================
// Factory — generated by DDF Engine
// Domain: base.org_structure
// ============================================================

model Factory {
  id          Int      @id @default(autoincrement())
        factory_code     String @unique
      factory_name     String
      status           String
      

  created_by  Int?
  created_at  DateTime @default(now())
  updated_at  DateTime @updatedAt

  @@map("org_factory")
}
