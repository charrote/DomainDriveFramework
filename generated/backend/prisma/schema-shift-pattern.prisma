// ============================================================
// ShiftPattern — generated by DDF Engine
// Domain: base.shift
// ============================================================

model ShiftPattern {
  id          Int      @id @default(autoincrement())
        pattern_name     String @unique
      description      String
  

  created_by  Int?
  created_at  DateTime @default(now())
  updated_at  DateTime @updatedAt

  @@map("shift_pattern")
}
