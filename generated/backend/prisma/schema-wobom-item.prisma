// ============================================================
// WOBOMItem — generated by DDF Engine
// Domain: base.work_order_bom
// ============================================================

model WOBOMItem {
  id          Int      @id @default(autoincrement())
        work_order_no    Int
      material_code    String
      material_name    String
      required_qty     Float
      consumed_qty     Float
      unit             String
      

  created_by  Int?
  created_at  DateTime @default(now())
  updated_at  DateTime @updatedAt

  @@map("wo_bom_item")
}
