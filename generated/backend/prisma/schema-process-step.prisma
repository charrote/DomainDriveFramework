// ============================================================
// ProcessStep — generated by DDF Engine
// Domain: base.process_step
// ============================================================

model ProcessStep {
  id          Int      @id @default(autoincrement())
        process_route_id Int
      step_no          Int
      step_code        String
      step_name        String
      standard_time_minutes Int
      device_type_required String
      workstation_type String
      description      String
          
  // Relation: process_route → ProcessRoute

  created_by  Int?
  created_at  DateTime @default(now())
  updated_at  DateTime @updatedAt

  @@map("process_step")
}
