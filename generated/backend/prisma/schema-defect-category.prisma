// ============================================================
// DefectCategory — generated by DDF Engine
// Domain: base.defect
// ============================================================

model DefectCategory {
  id          Int      @id @default(autoincrement())
        category_code    String
      category_name    String
      parent_id        Int
  

  created_by  Int?
  created_at  DateTime @default(now())
  updated_at  DateTime @updatedAt

  @@map("defect_category")
}
