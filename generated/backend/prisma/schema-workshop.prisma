// ============================================================
// Workshop — generated by DDF Engine
// Domain: base.org_structure
// ============================================================

model Workshop {
  id          Int      @id @default(autoincrement())
        factory_id       Int
      workshop_code    String
      workshop_name    String
      status           String
  

  created_by  Int?
  created_at  DateTime @default(now())
  updated_at  DateTime @updatedAt

  @@map("org_workshop")
}
