// ============================================================
// ProductionRecord — generated by DDF Engine
// Domain: base.production_record
// ============================================================

model ProductionRecord {
  id          Int      @id @default(autoincrement())
        record_no        String
      work_order_no    Int
      process_step_id  Int
      operator_id      Int
      device_id        Int
      shift_id         Int
      production_line_id Int
      start_time       DateTime
      end_time         DateTime
      output_quantity  Int
      scrap_quantity   Int
      status           String
      remark           String
          
  // Relation: work_order → WorkOrder
  // Relation: process_step → ProcessStep
  // Relation: operator → Operator
  // Relation: device → Device
  // Relation: shift → Shift

  created_by  Int?
  created_at  DateTime @default(now())
  updated_at  DateTime @updatedAt

  @@map("production_record")
}
