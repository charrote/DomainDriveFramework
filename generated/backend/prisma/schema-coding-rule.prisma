// ============================================================
// CodingRule — generated by DDF Engine
// Domain: base.coding_rule
// ============================================================

model CodingRule {
  id          Int      @id @default(autoincrement())
        rule_code        String @unique
      rule_name        String
      entity_type      String
      prefix           String
      date_format      String
      seq_digits       Int
      seq_strategy     String
      seq_scope        String
      sample_output    String
      status           String
      

  created_by  Int?
  created_at  DateTime @default(now())
  updated_at  DateTime @updatedAt

  @@map("coding_rule")
}
